import React from 'react';
import './Cart.css';

function Cart() {
    return (
        <div className="cartBody">
        <h1 className="cartTitle">Carrito</h1>
        </div>
    );
}

export default Cart;